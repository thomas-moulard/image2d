#ifndef IMAGE_HXX_
# define IMAGE_HXX_

template<typename E, typename Data>
Image<E, Data>::Image ()
{
}

template<typename E, typename Data>
Image<E, Data>::~Image ()
{
}

template<typename E, typename Data>
vtype(E, iterator)
Image<E, Data>::first ()
{
  //  return this->exact(E).impl_first ();
}

template<typename E, typename Data>
vtype(E, iterator)
Image<E, Data>::last ()
{
  //  return this->exact(E).impl_last ();
}

#endif /* !IMAGE_HXX_ */
